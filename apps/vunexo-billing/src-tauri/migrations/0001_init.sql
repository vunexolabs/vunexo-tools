-- Round 1 foundation migration. Intentionally has no business tables — it
-- exists only to prove the SQLx migration workflow end-to-end. Round 3
-- (database schema) adds the real tables.
SELECT 1;
