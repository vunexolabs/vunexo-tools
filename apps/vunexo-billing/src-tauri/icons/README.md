Placeholder icon set (flat slate square, no logo) — generated with `pnpm tauri icon` so `cargo build`/`generate_context!()` has real files to embed. Replace with the real Vunexo Billing logo before release:

```
pnpm tauri icon path/to/real-logo.png
```
