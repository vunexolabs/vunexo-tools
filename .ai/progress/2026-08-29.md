# 2026-08-29

Reconstructed from `git log` — predates the progress-log system.

## What happened

`3e480f4` Round 7: Invoices vertical slice — create, issue, cancel, duplicate, list. Includes Settings backend and invoice numbering (`InvoiceNumberSequencer` with a non-reserving `preview_next` vs. transaction-bound `issue_next`, so a failed issue never burns a number).

Verified with 4 real-SQLite integration tests: snapshot integrity (editing a customer after issue must not change the issued invoice — proven by editing the live customer mid-test and reloading), sequential numbering never reused, calculation-engine output matching the design doc's hand-verified vectors exactly.

User manually tested Business Setup + Customers CRUD via `pnpm tauri dev` and confirmed working.

## Verify

`git show 3e480f4 --stat`
